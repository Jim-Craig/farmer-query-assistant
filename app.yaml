name: farmer-query-assistant

# Databricks Apps provide OAuth authentication automatically
# No need to configure DATABRICKS_HOST or DATABRICKS_TOKEN

# Optional: Specify compute configuration
# resources:
#   driver:
#     instance_pool_id: "your-pool-id"
